---
session: 1
dev: eric
slug: main
branch: main
started: 2026-08-14T04:15:58Z
ended:
points:
pr_numbers: [6]
status: open
transcript: /home/eric/.claude/projects/-home-eric-chiplog/7933e635-f51f-5c7b-a20f-4fd37e98faa9.jsonl
---

# Session 1 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: DEC-011 — chiplog becomes a native store-published app

**Completed:**
- Wrote `docs/decisions/DEC-011-chiplog-becomes-a-native-store-published-app.md` recording the direction change: rebuild on Expo / React Native (SDK 57), TypeScript strict, matching `../bushel-mobile`; published to the App Store and Google Play; PWA retired.
- Declared three amendments, generated in both directions by `npm run gen:decisions`: **reverses** DEC-001 (vanilla/no-build — every leg dies), **retires** DEC-007's service-worker leg only, **revises** DEC-006 (bundle-don't-fetch stands, `expo-font` replaces `@font-face`).
- DEC-004 / DEC-005 / DEC-009 left untouched on purpose — the averaging maths is the asset that ports.
- Parked five open questions rather than deciding them: bundle identifier, foreground-vs-background location, privacy-policy hosting, `.claude/project-type`, and DEC-007's version-lockstep successor.
- Delivered the rewrite ordering plan in-session (not committed): store paperwork is the critical path because enrollment, Play's closed-testing period and device acquisition are wall-clock waits; the port runs underneath them.

**Code review:** 7 findings, all addressed in 84e84bc. Two mattered. (1) The `app.js` line citations were **wrong** — `:54-119` swept in `onPosition`/`onError`, which call `render()` and `showMsg()`; now cites the four pure functions individually. (2) DEC-007 holds two legs and the original scope retired both; the version-lockstep leg is unsettled, not dead, and is now parked. Also: `refines` → `revises` on DEC-006, recurring $99/yr added to the Tradeoff, `Revisit if` rewritten to name a condition that would actually revisit this holding.
**PR:** [PR #6](https://github.com/mobiustripper42/chiplog/pull/6)
**Points:** 3
**Branch:** task/native-rewrite-direction
**Opened at:** 2026-08-14T04:15:58Z

**Next Steps:**
- `docs/SPEC.md` § Tech and § Deployment are materially false (still describe the PWA) and `amends_spec` cannot reach them — `scripts/check-decisions.mjs:125` requires a numbered section and chiplog's SPEC has none. Needs a direct edit.
- `docs/PROJECT_PLAN.md` still describes PWA phases 1–3. Needs a full rewrite around the new ordering: start the store clocks (Apple enrollment, Play account, iPhone, bundle ID, public privacy policy) in parallel with extracting the maths to a tested TS module.
- Verify before planning around them: current Play closed-testing tester count and duration; **whether that requirement is per-account or per-app** (if per-account, chiplog's rehearsal clears muster's path entirely); Expo SDK 57 minimum iOS version (buy the used iPhone *after* this); whether SDK 57 auto-generates the iOS privacy manifest for async-storage.
- Consider an organization Play account for muster (D-U-N-S) — the closed-testing requirement has historically applied to individual accounts only, and account type is not easily changed after paying.

**Context:**
- Reviewer-can't-test-it risk: chiplog's whole function is unobservable without motion. An App Reviewer at a desk sees `—` or `0.0`. Same shape as the demo-credentials problem, arriving through a different door — mitigate with App Review Notes plus the already-existing accuracy / fix-count / window-fill readouts, which visibly move while stationary.
- Write `docs/STORE_PIPELINE.md` *during* the work, not after. The runbook is what muster inherits and is the actual deliverable of this project.
- Recommended build order: Android first (no enrollment wait, no device to buy, the Pixel is already the target), iOS second via TestFlight.
- Keep the PWA working during the rewrite: `git mv` it to `legacy-pwa/` in the same commit that scaffolds Expo, since `package.json` gets replaced wholesale. Delete it only after native parity is observed on-device.
- Neither repo has an `eas.json` and `eas` is not on PATH — the EAS side is greenfield in both chiplog and bushel-mobile.
- bushel-mobile is parked mid-phase: Phase 1.1 (login + orders) merged as its PR #13, never verified on a device.

**Context:**
